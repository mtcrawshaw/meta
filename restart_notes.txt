Goal: Minimize diff with develop branch while keeping performance

Tasks to convert ikostrikov code into our format:
    - make_env vs. get_env and add MetaWorld envs
    - Method definitions in ppo.py
    - Method definitions in storage.py
    - Method definitions in utils.py
    - Call to .train() in network?
    - Metric formatting/output in train.py
    - Argument names/formatting in main.py (normalize advantages?)
    - Typing, comments, and docstrings
    - Minimize diff for all files in meta/

Conversion tasks to leave for later:
    - Unwrap enviroment
